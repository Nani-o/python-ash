#!/usr/bin/env python

"""Shared constants for AAP object type names used across ash."""

JOB_TEMPLATES = 'job_templates'
JOBS = 'jobs'
PROJECTS = 'projects'
INVENTORIES = 'inventories'
HOSTS = 'hosts'

CACHED_OBJECT_TYPES = (JOB_TEMPLATES, PROJECTS, INVENTORIES)